{
  "name": "cli-coding-agent",
  "version": "1.7",
  "type": "module",
  "bin": {
    "sonar": "./src/Handler.ts"
  },
  "scripts": {
    "start": "bun run src/Handler.ts",
    "dev": "bun --watch run src/Handler.ts",
    "build": "bun build src/index.ts --outdir ./dist --target node"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}